{
  "name": "naval-radar-reverse-proxy",
  "version": "1.0.0",
  "description": "Simple web page that allows the user to control the radar simulator",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "jsurrea",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.7",
    "ws": "^8.18.0"
  }
}
